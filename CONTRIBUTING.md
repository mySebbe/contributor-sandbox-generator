# Contributing

Add or update tests for every stack detection or template change. Generated files should stay conservative and avoid project-specific assumptions.

Run:

```bash
python -m unittest discover -s tests
```

Release instructions live in [PUBLISHING.md](PUBLISHING.md).
